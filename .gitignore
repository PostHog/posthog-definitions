# Direnv — local credentials, never committed
.envrc
.direnv/

# Dotenv variants
.env
.env.local
.env.*.local

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
dist/
*.tsbuildinfo

# Smoke test scratch dir
.smoke-work/

# Integration test scratch dirs (need to live inside the repo so
# `@posthog/definitions` self-imports resolve via the package's exports)
.apply-int-*/
.pull-int-*/
.dump-int-*/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Claude Code per-project state — track shared skills, ignore local settings
.claude/*
!.claude/skills/
